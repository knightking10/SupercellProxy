using System;

namespace SupercellProxy
{
    /// <summary>
    /// Specifies if a packet is client/server sided
    /// </summary>
    public enum PacketDestination
    {
        FROM_CLIENT,
        FROM_SERVER
    }
}
